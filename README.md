# Proxy Manager

This is a forked version of the https://github.com/Ocramius/ProxyManager library, enhanced with support for PHP 8.1+. It also adds the capability to define a callback function that’s executed when a proxied method throws an exception.
